public with sharing class RequestModigieSupportContact 
{
    // @Depricated
    // public static Contact getInfo(String recId)
    // {
    //     Contact con = [SELECT Id, FirstName, LastName, Email, Account.Name, modigie__linkedin_url__c, modigie__Modigie_Verified_Number__c FROM Contact WHERE Id =: recId];
        
    //     return con;
    // }

}